<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>👋 Give me a title 🔥</title>
    <!-- https://github.com/joshbuchea/HEAD -->

    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="description" content="description of your site">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!--[if lt IE 9]>
      <script src="https://cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.3/html5shiv.min.js"></script>
    <![endif]-->
  </head>

  <body>

    <a
      class="underline--hover"
      style='
        font-size: 40px;
        cursor: pointer;
      '

    > test linqqkyyyyjj
    <br> multine link
  </a>
    <br>
    <a
      class="underline--link"
      style='
        font-size: 40px;
        cursor: pointer;
      '

    > test linqqkyyyyjj
    <br> multine link
  </a>


    <h3>Comment est votre blanquette ?</h3>
    <% users =  ['Athos', 'Porthos', 'Aramis', '', '', '']; %>
    <% users.forEach(function(user){ %>
      <div class="section" style="margin: 100px 0; height: 50vh; width: 100%; background: rgba(<%= Math.ceil(Math.random() * 255) %>, <%= Math.ceil(Math.random() * 255) %>, <%= Math.ceil(Math.random() * 255) %>, 255)">
        <p><%= user %></p>

    <% listTest.forEach(function(item){ %>
      <p class="text-<%= item %>">test text-<%= item %></p>
    <% }); %>

    <h3>Test alias img tag</h3>
    <img class="img-<%= imgSizes.medium %>" src="<%= $img %>/example.jpg">


    <h3>Test alias sass image</h3>
    <div class="img-<%= imgSizes.small %>" id="testImg"></div>

    <!--SCRIPTS are auto-imported-->
  </body>
</html>